# 研究生周报助手 V1\.0 产品需求文档（PRD）

## 1\. 项目概述

### 1\.1 产品定位

轻量化课题组学术周报管理工具，V1\.0 聚焦核心闭环：**导师创建学生账号 → 学生填写/提交周报 → 导师在线批阅 → 双向查看历史记录**。替代微信、文档、邮件零散汇报方式，实现学术进度统一归档、可追溯、可管理。

本版本为极简MVP，仅服务单课题组，无多租户、无消息通知、无文件附件、无数据导出、无统计图表。

### 1\.2 目标用户

- **导师**：批量管理名下学生、批阅周报、查看学生学术进度

- **研究生**：定期填写学术周报、接收导师评语、回溯个人学习记录

### 1\.3 V1\.0 核心约束（硬性）

- 仅两种角色：导师、学生，无管理员前台功能，后台手动创建导师账号

- 学生账号**仅可由导师创建**，无自助注册功能

- 学生账号为手机号，初始密码为手机号后6位

- 学生首次登录**强制修改初始密码**，修改前禁止进入系统任何页面

- 周报提交后学生不可编辑、不可修改，仅只读查看

- 严格权限隔离：学生仅查看自己周报，导师仅查看归属自己的学生周报

## 2\. 整体业务流程

1. 后台手动创建导师账号

2. 导师登录系统，输入学生手机号，批量/单个创建学生账号，学生自动归属该导师

3. 学生使用手机号\+后6位初始密码登录，系统强制跳转改密页面

4. 学生修改密码成功后，进入系统，可新建周报、保存草稿、提交周报

5. 导师查看名下学生列表及周报状态，对已提交周报填写评语、完成批阅

6. 学生查看批阅结果，双方可永久查看所有历史周报记录

## 3\. 角色权限说明

### 3\.1 学生角色（student）

可操作：登录、修改初始密码、新建周报、保存周报草稿、提交周报、查看个人周报列表、查看周报详情、查看导师评语。

不可操作：创建账号、查看其他学生周报、修改已提交周报、批阅周报、访问导师功能页面。

### 3\.2 导师角色（teacher）

可操作：登录、创建学生账号、查看名下所有学生列表、查看学生周报状态、查看学生周报详情、填写/保存周报批阅评语、查看学生所有历史周报。

不可操作：创建导师账号、修改学生密码、编辑学生周报内容、查看其他导师的学生数据。

## 4\. 功能需求详情

### 4\.1 登录模块（通用）

#### 4\.1\.1 账号密码登录

- 登录账号：手机号

- 密码：用户自定义密码（首次为手机号后6位）

- 鉴权方式：JWT令牌

- 登录成功返回：token、用户角色、用户ID、**need\_change\_pwd**（是否需要强制改密）

#### 4\.1\.2 强制改密逻辑（学生专属）

- 学生账号为初始密码时，登录成功后前端强制拦截路由，跳转改密页面

- 未完成改密，禁止访问所有业务页面

- 改密成功后，更新数据库标识，清空原有token，强制重新登录

### 4\.2 密码修改模块

接口：修改密码，传入旧密码、新密码

逻辑：校验旧密码正确性，加密存储新密码，重置初始密码标识为已修改

### 4\.3 导师专属：学生账号创建

- 操作入口：导师首页【创建学生账号】按钮

- 输入参数：学生手机号

- 校验规则：手机号格式校验、手机号唯一校验（不可重复创建）

- 自动生成规则：账号=手机号，初始密码=手机号后6位（加密存储）

- 自动绑定关系：新学生的teacher\_id=当前登录导师ID

- 初始状态：is\_init\_password=1（需强制改密），role=student

### 4\.4 学生周报模块

#### 4\.4\.1 周报字段（固定模板，V1\.0无自定义字段）

- 周报周期（手动选择：年\-周，如2026\-34）

- 本周完成工作

- 遇到的问题与难点

- 下周工作计划

- 本周文献阅读总结

#### 4\.4\.2 周报状态枚举

- draft：草稿（可反复编辑保存，仅自己可见）

- submitted：已提交待批阅（不可编辑，导师可查看批阅）

- reviewed：已批阅（导师已填写评语，最终归档状态）

#### 4\.4\.3 核心操作

- 保存草稿：不改变状态，可多次编辑覆盖

- 提交周报：状态由draft→submitted，锁定编辑权限

- 查看个人周报列表：按时间倒序展示，显示周期、状态、提交时间

- 查看周报详情：展示个人填写内容\+导师评语（如有）

### 4\.5 导师批阅模块

#### 4\.5\.1 学生总览

导师首页展示名下所有学生列表，每个学生展示最新周报状态，快速识别未提交、待批阅学生。

#### 4\.5\.2 周报批阅功能

- 可查看学生完整周报内容

- 输入导师评语，点击保存

- 保存后周报状态由submitted→reviewed，记录批阅时间

- 支持查看该学生所有历史周报记录

## 5\. 页面路由清单（V1\.0 全部页面）

1. `/login` 登录页

2. `/change-pwd` 强制修改密码页（学生专属拦截页）

3. `/student/home` 学生首页（周报列表\+新建周报）

4. `/student/report/edit` 周报编辑/草稿保存页

5. `/student/report/:id` 周报详情页（查看评语）

6. `/teacher/home` 导师首页（学生列表\+创建学生账号）

7. `/teacher/report/:id` 周报批阅详情页

## 6\. 数据库表结构（可直接执行SQL）

### 6\.1 用户表 user

```sql
CREATE TABLE `user` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `username` varchar(20) NOT NULL COMMENT '登录账号（手机号）',
  `password` varchar(100) NOT NULL COMMENT '加密密码',
  `role` varchar(16) NOT NULL COMMENT '角色：student/teacher',
  `teacher_id` bigint DEFAULT NULL COMMENT '学生所属导师ID，导师为空',
  `is_init_password` tinyint(1) NOT NULL DEFAULT 1 COMMENT '1=初始密码需改密，0=已修改',
  `create_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 6\.2 周报表 weekly\_report

```sql
CREATE TABLE `weekly_report` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `student_id` bigint NOT NULL COMMENT '学生用户ID',
  `week_year` varchar(20) NOT NULL COMMENT '周报周期 例：2026-34',
  `content_work` text NOT NULL COMMENT '本周工作内容',
  `content_problem` text NOT NULL COMMENT '遇到的问题',
  `content_next` text NOT NULL COMMENT '下周计划',
  `content_literature` text NOT NULL COMMENT '文献阅读情况',
  `status` varchar(20) NOT NULL DEFAULT 'draft' COMMENT 'draft/submitted/reviewed',
  `teacher_comment` text DEFAULT NULL COMMENT '导师批阅评语',
  `create_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `submit_at` datetime DEFAULT NULL COMMENT '提交时间',
  `review_at` datetime DEFAULT NULL COMMENT '批阅时间',
  PRIMARY KEY (`id`),
  KEY `idx_student_id` (`student_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学生周报表';
```

## 7\. 后端API接口清单（AI Coding 直接对接）

### 7\.1 通用接口

- `POST /api/login` 账号密码登录

- `POST /api/user/change-pwd` 修改密码

- `GET /api/user/info` 获取当前登录用户信息

### 7\.2 学生接口

- `GET /api/report/list` 获取个人周报列表

- `POST /api/report/save` 保存周报草稿/新建周报

- `PUT /api/report/submit/:id` 提交周报

- `GET /api/report/:id` 获取周报详情

### 7\.3 导师接口

- `POST /api/teacher/create-student` 创建学生账号

- `GET /api/teacher/students` 获取名下所有学生列表

- `GET /api/teacher/student/:stuId/reports` 获取指定学生所有周报

- `PUT /api/teacher/report/:reportId/review` 周报批阅、保存评语

## 8\. 非功能需求（V1\.0）

- 密码必须使用BCrypt加密存储，禁止明文存储

- 严格权限拦截：前端路由拦截 \+ 后端接口双重权限校验

- 初始密码逻辑、改密强制拦截逻辑优先级最高

- 已提交周报禁止前端编辑、禁止后端修改内容

## 9\. 明确排除V1\.0的功能（不开发）

附件上传、周报导出、消息通知、邮件提醒、截止时间管控、数据统计、多租户、自助注册、密码找回、批量批阅、打分评级、小组管理、移动端适配。

> （注：部分内容可能由 AI 生成）
