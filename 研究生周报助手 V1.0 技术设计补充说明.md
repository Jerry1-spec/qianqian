# 研究生周报助手 V1.0 技术设计补充说明

> 本文档是《研究生周报助手 V1.0 产品需求文档（PRD）》的配套技术补充，用于补齐 PRD 中未明确的技术细节，作为开发的直接依据。
> PRD 与本文档冲突时，以本文档为准（本文档不修改 PRD 的产品范围，仅细化实现）。

---

## 0. 技术栈选型（本次确定）

| 层 | 技术 | 版本建议 | 说明 |
|---|---|---|---|
| 后端框架 | Spring Boot | 3.2.x | JDK 17 |
| 持久层 | MyBatis-Plus | 3.5.x | 简化 CRUD |
| 数据库 | MySQL | 8.0 | 对应 PRD 建表 SQL |
| 鉴权 | JWT（jjwt） | 0.12.x | 无状态令牌 |
| 密码加密 | BCrypt | Spring Security Crypto | 符合 PRD 8.1 |
| 参数校验 | Jakarta Validation | - | 手机号等格式校验 |
| 前端框架 | Vue | 3.4.x | Composition API + `<script setup>` |
| 构建工具 | Vite | 5.x | - |
| UI 组件库 | Element Plus | 2.x | 表格、表单、弹窗 |
| 路由 | Vue Router | 4.x | 支持路由守卫拦截 |
| 状态管理 | Pinia | 2.x | 存 token、用户信息 |
| HTTP | Axios | 1.x | 统一拦截器 |

> 说明：为保持 MVP 轻量，后端**不引入完整 Spring Security 过滤器链**，仅使用其 `BCryptPasswordEncoder`；鉴权改用自定义 JWT 拦截器（`HandlerInterceptor`），降低复杂度。

---

## 1. 缺口修复项（对应上一轮提出的 6 个问题）

### 1.1 导师账号「后台手动创建」的具体方式

采用 **初始化 SQL 脚本** 方式，随项目提供 `sql/init.sql`。

- 导师账号由运维/开发直接执行 SQL 插入，V1.0 不提供任何前台/接口创建导师的能力。
- 密码需为 BCrypt 密文，不可直接写明文。提供一个一次性工具类 `PasswordHashTool`（`main` 方法）用于生成密文，或使用在线 BCrypt 工具。
- 导师账号 `role='teacher'`，`teacher_id=NULL`，`is_init_password=0`（导师无强制改密流程）。

示例（密文需替换为 `PasswordHashTool` 生成的真实值）：

```sql
-- 创建初始导师账号，账号为手机号，密码明文示例：Teacher@123（请用工具生成密文后替换）
INSERT INTO `user` (`username`, `password`, `role`, `teacher_id`, `is_init_password`)
VALUES ('13800000000', '$2a$10$REPLACE_WITH_REAL_BCRYPT_HASH', 'teacher', NULL, 0);
```

### 1.2 `weekly_report` 表增加 `(student_id, week_year)` 唯一约束

**产品决策：同一学生同一周报周期只能存在一条记录（不允许重复建报）。**

在 PRD 原建表语句基础上，追加唯一索引：

```sql
UNIQUE KEY `uk_student_week` (`student_id`, `week_year`)
```

- 后端在 `POST /api/report/save` 新建时先按 `(student_id, week_year)` 查重；已存在则走「更新」逻辑而非「新建」。
- 触发唯一约束冲突时，返回业务错误码 `40901`（见第 4 节），提示「该周期周报已存在」。

### 1.3 `POST /api/report/save` 的编辑校验规则

该接口同时承担「新建草稿」与「保存已有草稿」，后端强制校验：

1. 请求含 `id` → 视为更新：
   - 校验该周报存在且 `student_id == 当前登录用户`（越权则 403）。
   - 校验当前 `status == 'draft'`；若为 `submitted` / `reviewed`，**拒绝并返回 `40301`**（呼应 PRD 8.4「已提交周报禁止后端修改内容」）。
2. 请求不含 `id` → 视为新建：
   - 走 1.2 的查重逻辑。
   - 初始 `status='draft'`，`create_at` 由 DB 默认值生成。
3. 无论新建/更新，`teacher_comment`、`status`、`submit_at`、`review_at` 等字段**不接受前端传入**，由后端控制。

### 1.4 JWT 令牌规格与「改密后强制原 token 失效」机制

**Token 结构（Payload claims）：**

| claim | 含义 |
|---|---|
| `sub` | 用户 ID |
| `role` | student / teacher |
| `pv`  | password version（密码版本号，见下） |
| `iat` / `exp` | 签发 / 过期时间 |

- **有效期**：8 小时（`exp = iat + 8h`）。V1.0 不做 refresh token。
- **`is_init_password` 不放进 token**，改由登录接口 `POST /api/login` 的响应体返回 `need_change_pwd`，前端据此拦截路由（PRD 4.1.1 / 4.1.2）。
- **原 token 强制失效机制（对应 PRD 4.1.2「清空原有 token」）**：
  - `user` 表新增字段 `pwd_version int NOT NULL DEFAULT 0`（见第 2 节 DDL 变更）。
  - 登录签发 token 时把当前 `pwd_version` 写入 claim `pv`。
  - 每次修改密码成功后，`pwd_version = pwd_version + 1`。
  - JWT 拦截器校验：token 中的 `pv` 必须等于库中当前 `pwd_version`，否则判定令牌失效（`40101`），强制重新登录。
  - 这样改密后所有旧 token 立即作废，无需引入 Redis 黑名单，符合 MVP 轻量原则。

### 1.5 周报「编辑/新建」入口的路由与传参

- 新建周报：`/student/report/edit`（不带参数）。
- 编辑已有草稿：`/student/report/edit?id={reportId}`（query 传参）。
- 编辑页 `onMounted` 读取 `route.query.id`：有则调 `GET /api/report/:id` 回填表单；无则空白新建。
- 仅 `status='draft'` 的周报允许进入编辑页；`submitted/reviewed` 只能进详情页 `/student/report/:id`（只读）。

### 1.6 技术栈补充（已在第 0 节完成）

---

## 2. 数据库 DDL 变更汇总

在 PRD 第 6 节基础上，V1.0 最终以下述为准。

### 2.1 `user` 表（新增 `pwd_version` 字段）

```sql
CREATE TABLE `user` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `username` varchar(20) NOT NULL COMMENT '登录账号（手机号）',
  `password` varchar(100) NOT NULL COMMENT 'BCrypt加密密码',
  `role` varchar(16) NOT NULL COMMENT '角色：student/teacher',
  `teacher_id` bigint DEFAULT NULL COMMENT '学生所属导师ID，导师为空',
  `is_init_password` tinyint(1) NOT NULL DEFAULT 1 COMMENT '1=初始密码需改密，0=已修改',
  `pwd_version` int NOT NULL DEFAULT 0 COMMENT '密码版本号，改密后+1使旧token失效',
  `create_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 2.2 `weekly_report` 表（新增唯一约束）

```sql
CREATE TABLE `weekly_report` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `student_id` bigint NOT NULL COMMENT '学生用户ID',
  `week_year` varchar(20) NOT NULL COMMENT '周报周期 例：2026-34',
  `content_work` text NOT NULL COMMENT '本周工作内容',
  `content_problem` text NOT NULL COMMENT '遇到的问题',
  `content_next` text NOT NULL COMMENT '下周计划',
  `content_literature` text NOT NULL COMMENT '文献阅读情况',
  `status` varchar(20) NOT NULL DEFAULT 'draft' COMMENT 'draft/submitted/reviewed',
  `teacher_comment` text DEFAULT NULL COMMENT '导师批阅评语',
  `create_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `submit_at` datetime DEFAULT NULL COMMENT '提交时间',
  `review_at` datetime DEFAULT NULL COMMENT '批阅时间',
  PRIMARY KEY (`id`),
  KEY `idx_student_id` (`student_id`),
  UNIQUE KEY `uk_student_week` (`student_id`, `week_year`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学生周报表';
```

---

## 3. 接口契约细化

统一响应体：

```json
{ "code": 0, "msg": "success", "data": {} }
```

- `code=0` 表示成功，非 0 为业务错误（见第 4 节）。
- 除 `POST /api/login` 外，所有接口需在 Header 携带 `Authorization: Bearer <token>`。

### 3.1 `POST /api/login`

请求：`{ "username": "13800000000", "password": "xxx" }`
响应 data：`{ "token": "...", "role": "student", "userId": 12, "need_change_pwd": true }`

### 3.2 `POST /api/user/change-pwd`

请求：`{ "oldPassword": "xxx", "newPassword": "yyy" }`
逻辑：校验旧密码 → BCrypt 加密新密码 → `is_init_password=0` → `pwd_version+1` → 返回成功（前端随即清 token 跳登录）。
新密码规则：长度 6–20，不与旧密码相同。

### 3.3 `POST /api/report/save`

请求：`{ "id": null, "weekYear": "2026-34", "contentWork": "...", "contentProblem": "...", "contentNext": "...", "contentLiterature": "..." }`
逻辑见 1.3。响应 data：`{ "id": 100 }`

### 3.4 `PUT /api/report/submit/:id`

逻辑：校验归属 + `status='draft'` → 置 `status='submitted'`、`submit_at=now`。非 draft 返回 `40301`。

### 3.5 `PUT /api/teacher/report/:reportId/review`

请求：`{ "comment": "评语内容" }`
逻辑：校验该周报所属学生的 `teacher_id == 当前导师` + `status='submitted'` → 写 `teacher_comment`、`status='reviewed'`、`review_at=now`。

### 3.6 `POST /api/teacher/create-student`

请求：`{ "phone": "13800000001" }`
逻辑：手机号格式（`^1[3-9]\d{9}$`）+ 唯一校验 → `username=phone`，`password=BCrypt(phone后6位)`，`role='student'`，`teacher_id=当前导师`，`is_init_password=1`。

其余接口（`GET /api/user/info`、`GET /api/report/list`、`GET /api/report/:id`、`GET /api/teacher/students`、`GET /api/teacher/student/:stuId/reports`）按 PRD 语义实现，均需做归属/角色权限校验。

---

## 4. 业务错误码约定

| code | 含义 |
|---|---|
| 0 | 成功 |
| 40001 | 参数校验失败（手机号格式、字段缺失等） |
| 40101 | 未登录 / token 无效或已失效（含改密后旧 token） |
| 40102 | 账号或密码错误 |
| 40301 | 无权限 / 状态不允许操作（如编辑已提交周报） |
| 40901 | 资源冲突（手机号已存在、该周期周报已存在） |
| 50000 | 服务器内部错误 |

---

## 5. 权限校验矩阵（后端强制，前端路由守卫辅助）

| 接口 | 允许角色 | 额外归属校验 |
|---|---|---|
| /api/login、/api/user/change-pwd、/api/user/info | 全部 | - |
| /api/report/* | student | 周报 `student_id == 当前用户` |
| /api/teacher/create-student、/api/teacher/students | teacher | - |
| /api/teacher/student/:stuId/reports | teacher | 目标学生 `teacher_id == 当前导师` |
| /api/teacher/report/:reportId/review | teacher | 周报所属学生 `teacher_id == 当前导师` |

> 强制改密拦截：`need_change_pwd=true` 的学生，前端路由守卫仅放行 `/change-pwd`；后端对除 `change-pwd`/`info`/`login` 外的业务接口，检测到 `is_init_password=1` 时返回 `40301`。

---

## 6. 建议的工程目录结构

### 后端 `report-backend/`
```
src/main/java/com/report/
├── ReportApplication.java
├── config/           # JWT拦截器注册、跨域、MyBatis-Plus配置
├── controller/       # LoginController、UserController、ReportController、TeacherController
├── service/          # 业务逻辑
├── mapper/           # MyBatis-Plus Mapper
├── entity/           # User、WeeklyReport
├── dto/              # 请求/响应对象
├── common/           # Result统一响应、ErrorCode、异常处理
└── util/             # JwtUtil、PasswordHashTool
src/main/resources/
├── application.yml
sql/
├── init.sql          # 建表 + 初始导师账号
```

### 前端 `report-frontend/`
```
src/
├── main.js
├── App.vue
├── router/index.js   # 路由 + 全局守卫（登录态/改密拦截/角色）
├── store/user.js     # Pinia：token、role、needChangePwd
├── api/              # axios 封装 + 各模块接口
├── views/
│   ├── Login.vue
│   ├── ChangePwd.vue
│   ├── student/{Home.vue, ReportEdit.vue, ReportDetail.vue}
│   └── teacher/{Home.vue, ReportReview.vue}
└── components/
```

---

## 7. 开发里程碑建议（供排期参考）

1. **M1 环境与骨架**：建库执行 `init.sql`、后端 Spring Boot 骨架 + JWT 拦截器、前端 Vite 骨架 + 路由守卫。
2. **M2 登录闭环**：登录、强制改密、`user/info`，打通前后端鉴权与拦截。
3. **M3 学生周报**：新建/草稿/提交/列表/详情。
4. **M4 导师批阅**：学生列表、查看学生周报、批阅评语。
5. **M5 联调与自测**：按第 5 节权限矩阵逐项验证越权与状态锁定。

---

> （注：本文档为技术补充说明，部分内容可能由 AI 生成，落地前请结合实际评审。）
