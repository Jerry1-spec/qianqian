# 研究生周报助手 V1.0 运行说明

## 一、快速启动（H2 内存库演示，推荐先用这个看效果）

**方式 A：双击一键脚本**

双击项目根目录的 `start.bat`，会自动构建后端、装前端依赖并启动，弹出两个窗口：
- 「周报助手-后端(8080)」：等待出现 `Started ReportApplication`
- 「周报助手-前端(5173)」：等待出现 `Local: http://localhost:5173/`

然后浏览器访问：**http://localhost:5173**

> 关闭对应窗口即可停止服务。H2 为内存库，**重启后数据清空**，导师账号会自动重建。

**登录账号**
- 导师：账号 `13800000000` / 密码 `Teacher@123`
- 学生：由导师在「导师端」创建，初始密码 = 手机号后 6 位，首次登录强制改密。

---

## 二、环境依赖

| 组件 | 版本 | 说明 |
|------|------|------|
| JDK | 17 | 当前路径 `C:\Program Files\Microsoft\jdk-17.0.20.101-hotspot` |
| Maven | 3.9.9 | 便携版已放在 `.tools\apache-maven-3.9.9`，无需系统安装 |
| Node.js | 18+ | 当前路径 `C:\Program Files\nodejs` |
| MySQL | 8.0 | **仅持久化模式需要**，H2 演示模式无需安装 |

> 若上述 JDK / Node 安装路径与你机器不同，请编辑 `start.bat` 顶部的 `JAVA_HOME` 与 `NODE_DIR`。

---

## 三、切换到 MySQL 持久化模式（数据不丢，贴近正式部署）

H2 是内存库、重启即清空。需要数据持久化时，按以下步骤切到 MySQL：

**1. 安装 MySQL 8.0** 并启动服务。

**2. 建库建表 + 初始化导师账号**：执行 `report-backend\sql\init.sql`
（该脚本已内置 `Teacher@123` 的正确 BCrypt 密文，开箱即用。）

```bash
mysql -u root -p < report-backend\sql\init.sql
```

**3. 核对数据库连接**：编辑 `report-backend\src\main\resources\application.yml` 中 `mysql` 段的
`url` / `username` / `password`，改成你的实际值（默认 `root/root`、库名 `report_assistant`）。

**4. 以 mysql profile 启动后端**（二选一）：
- 改 `application.yml` 顶部 `spring.profiles.active: h2` → `mysql`，重新打包运行；或
- 启动时指定：`java -jar target\report-backend-1.0.0.jar --spring.profiles.active=mysql`

前端无需任何改动。

---

## 四、常见问题

- **登录报"服务器内部错误(50000)"**：多为数据库未就绪或哈希不匹配。H2 模式请确认后端已完全启动；MySQL 模式请确认 `init.sql` 已执行、连接配置正确。
- **端口被占用**：8080 或 5173 被占用时，先关掉占用进程，或修改 `application.yml`(server.port) 与前端 `vite.config.js` 的端口。
- **前端能打开但接口失败**：确认后端 8080 已启动；前端通过 Vite 代理 `/api → localhost:8080`。

---

## 五、目录说明

```
周报助手/
├── start.bat                 一键启动（H2 模式）
├── report-backend/           后端 Spring Boot
│   ├── sql/init.sql          MySQL 建库脚本（含正确导师密文）
│   └── src/main/resources/
│       ├── application.yml        默认 h2，含 mysql profile
│       ├── application-h2.yml      H2 数据源（NON_KEYWORDS=USER 规避保留字）
│       ├── schema-h2.sql / data-h2.sql   H2 建表与初始化
├── report-frontend/          前端 Vue3 + Vite
└── .tools/                   便携版 Maven（可删，删后需另装 Maven）
```
